#ifndef GAME_STATE_H
#define GAME_STATE_H

typedef enum GameState {
    STATE_MENU,
    STATE_GAME,
    STATE_CONTINUE,
    STATE_SCOREBOARD,
    STATE_SETTINGS,
    STATE_EXIT
} GameState;


#endif // GAME_STATE_H
